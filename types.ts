export interface FrontpageItem {
  id: string;
  name: string;
  imageUrl: string;
  linkUrl: string;
}

export interface ProjectspageItem {
  id: string;
  name: string;
  imageUrl: string;
  linkUrl: string;
}

export interface AboutpageItem {
  id: string;
  title: string;
  imageUrl: string;
  description: string;
}
